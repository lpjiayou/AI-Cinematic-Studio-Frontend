@AGENTS.md

# Claude repository instructions

`AGENTS.md` is the governing repository instruction source. This file only imports it
for Claude-compatible tooling and adds no independent implementation authority.
